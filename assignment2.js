// 1A.
const addTwoNumbers = (a, b) => a + b;

// 1B
const stringLength = (myStr) => (myStr.length < 10 ? "short" : "long");

// 2
// This function takes two arguments a and b. It compares them, and if a is greater than b, it logs the value of a. Otherwise, it logs the value of b

// 3
const numbers = [1, 2, 3, 4, 5];
const square = numbers.map(num => num ** 2);
console.log(squareRoot); // Output: [ 1, 4, 9, 16, 25 ]




